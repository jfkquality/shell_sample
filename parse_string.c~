#include <stdio.h>
#include <string.h>

int main(void)
{
  int i = 0;
  char *str = "My name is john knight";
  char *ptr;
  char str_arr[] strlen(str);

  ptr = str;
  while (ptr)
  {
    ptr = strtok(ptr, " ");
    str_arr[i] = *ptr; 
    i++;
  }
     return (0);
}
