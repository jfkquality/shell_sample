#include "holberton.h"
#include <stdio.h>

/**
 *  _strlen - recursion. print len of str
 * @s: string pointer parameter
 *
 * Return: void
 */
int _strlen(char *s)
{
	char *s1 = s;

	if (*s1 != '\0')
	{
		return (1 + _strlen(s1 + 1));
	}
	return (0);
}
